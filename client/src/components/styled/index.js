import styled from 'styled-components';

// re-usable components. Usage:
// import { Wrapper } from './styled';

export const Wrapper = styled.section`
  margin-top: 2rem;
  background-color: paleturquoise;
`;